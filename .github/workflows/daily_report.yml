name: Daily Stock Premarket Report

on:
  schedule:
    - cron: "35 12 * * 1-5"
  workflow_dispatch:
    inputs:
      ticker:
        description: "Ticker symbol, comma-separated list, or ALL"
        required: false
        default: "ALL"
        type: string
      report_type:
        description: "Type of report to generate"
        required: false
        default: "premarket"
        type: string

jobs:
  build-report:
    runs-on: ubuntu-latest

    env:
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      DEEPSEEK_MODEL: deepseek-v4-flash
      REPORT_TICKER: ${{ github.event.inputs.ticker || 'ALL' }}
      REPORT_TICKERS: ${{ github.event.inputs.ticker || 'ALL' }}
      REPORT_TYPE: ${{ github.event.inputs.report_type || 'premarket' }}
      GMAIL_CLIENT_ID: ${{ secrets.GMAIL_CLIENT_ID }}
      GMAIL_CLIENT_SECRET: ${{ secrets.GMAIL_CLIENT_SECRET }}
      GMAIL_REFRESH_TOKEN: ${{ secrets.GMAIL_REFRESH_TOKEN }}
      GMAIL_SENDER: ${{ secrets.GMAIL_SENDER }}
      EMAIL_TO: ${{ secrets.EMAIL_TO }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt

      - name: Generate report
        run: python src/main.py

      - name: Upload generated reports
        uses: actions/upload-artifact@v4
        with:
          name: daily-report
          path: |
            report/markdown/*.md
            report/html/*.html
            report/StockReportAnalysisToday.html
          if-no-files-found: warn
